services:
  #app:
  #  image: fiap-soat11:v2
  #  restart: always
  #  ports:
  #    - "5001:8080"
  #  environment:
  #    - AWS__Region=us-east-1
  #    - AWS__DynamoDB__ServiceURL=http://db:8000
  #    - AWS__DynamoDB__TableName=Pedidos
  #  depends_on:
  #    - db
  #  networks:
  #    - app-network
  db:
    image: amazon/dynamodb-local:latest
    restart: always
    ports:
      - "8000:8000"    
    networks:
      - app-network

networks:
  app-network:
    driver: bridge